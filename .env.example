# Variables d'environnement — CSE Divo (frontend)
# Copiez ce fichier en .env et adaptez l'URL selon votre déploiement.

# URL de l'API backend (server/) — en local, laissez la valeur par défaut.
VITE_API_URL=http://localhost:4000
